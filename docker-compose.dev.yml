# Override para ambiente de desenvolvimento local.
# Sobe banco + Keycloak para dev local (backend e frontend rodam fora do Docker).
# Uso: make dev-up

services:
  db:
    ports:
      - "127.0.0.1:5432:5432"

  keycloak:
    ports:
      - "127.0.0.1:10082:8080"
